{
  "name": "hospital-costing-tool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
 "install:all": "npm install --prefix server --include=dev && npm install --prefix client --include=dev",
        "build": "npm run install:all && npm run build --prefix client && node scripts/copy-client-dist.js",
    "seed": "npm run --prefix server seed",
    "start": "npm run --prefix server start"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}